import React, { ReactElement } from "react";
import Typography from "@material-ui/core/Typography";

function Graphics({}): ReactElement {
  return (
    <div>
      <Typography variant="h4" component="h2">
        Grafikler
      </Typography>
    </div>
  );
}

export default Graphics;
