import React, { ReactElement } from "react";
import Typography from "@material-ui/core/Typography";

interface Props {}

export default function Sites({}: Props): ReactElement {
  return (
    <div>
      <Typography>Siteler</Typography>
    </div>
  );
}
